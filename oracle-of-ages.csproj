<Project Sdk="Godot.NET.Sdk/4.7.1">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <TargetFramework Condition=" '$(GodotTargetPlatform)' == 'android' ">net9.0</TargetFramework>
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <Nullable>enable</Nullable>
    <RootNamespace>oracleofages</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <!-- The validation runner is built by the friend test assembly. -->
    <Compile Remove="validation\**\*.cs" />
    <!-- Import-time source models and their tests are separate tool projects. -->
    <Compile Remove="tools\OracleImporter\**\*.cs" />
    <Compile Remove="tools\OracleImporter.Tests\**\*.cs" />
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>oracle-of-ages.validation</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>
</Project>
